/*
 * New Archi Script
 */

console.log("New Archi Script");


// Loop on all views
$('view').each(function(view) {
    console.log(view);
})

// Loop on all concepts
$('concept').each(function(concept) {
  if ( concept.name == 'Car') {
    console.log(concept.name);

    //concept.prop().forEach(function(key) {
        console.log(concept.prop());
    //})

  }
})
